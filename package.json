{
  "name": "hyde-end-farm",
  "private": true,
  "description": "Hyde End Vineyard passport demo + Partner API (Vercel)",
  "scripts": {
    "build": "cd partner-api && npm install --include=dev && npm run build"
  }
}
